import Confirm from './confirm';
import ConfirmCancel from './confirmCancel';
import Navigate from './navigate';
import Send from './send';

const ModalList = { Date, Confirm, ConfirmCancel, Navigate, Send };

export default ModalList;
